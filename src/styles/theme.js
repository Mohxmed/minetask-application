import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#546fff",
      hover: "#3c54cc",
      lowHover: "#546fff29",
      text: "#ffffff",
    },
    secondary: {
      main: "#00c55b",
    },
  },
});

export default theme;
